apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gosqueal.fullname" . }}-test-connection"
  labels:
    {{- include "gosqueal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gosqueal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
