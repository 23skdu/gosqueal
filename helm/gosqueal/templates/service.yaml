apiVersion: v1
kind: Service
metadata:
  name: {{ include "gosqueal.fullname" . }}
  labels:
    {{- include "gosqueal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gosqueal.selectorLabels" . | nindent 4 }}
